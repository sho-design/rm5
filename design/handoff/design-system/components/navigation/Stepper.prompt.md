Pill stepper for 3-step flows: Details, Time, Confirm.

```jsx
<Stepper steps={['Details', 'Time', 'Confirm']} current={2} />
```

Current is ink with a sunshine numeral; done is sage with a check. Keep to 3 or 4 steps.
