import React from 'react';
export function PillNav({ brand = 'Restoration Medical', items, active, onSelect, onBook, bookLabel = 'Book', right, style }) {
  return React.createElement('nav', { style: { display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 16, padding: '14px 24px', background: 'var(--bg-page)', borderBottom: '1px solid var(--border-default)', fontFamily: 'var(--font-ui)', ...style } },
    React.createElement('a', { style: { display: 'flex', alignItems: 'center', gap: 10, textDecoration: 'none', color: 'var(--text-primary)', cursor: 'pointer' } }, React.createElement('span', { style: { width: 30, height: 30, borderRadius: 9, background: 'var(--rm-ink)', color: '#fff', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'var(--font-display)', fontWeight: 500, fontSize: 16 } }, brand[0]), React.createElement('span', { style: { fontSize: 18, fontWeight: 600, letterSpacing: '-.02em' } }, brand)),
    React.createElement('div', { style: { display: 'flex', gap: 4, padding: 4, borderRadius: 999, border: '1.5px solid var(--border-default)' } }, items.map(it => { const on = it === active; return React.createElement('a', { key: it, onClick: () => onSelect && onSelect(it), style: { padding: '9px 16px', borderRadius: 999, fontSize: 14, fontWeight: 600, cursor: 'pointer', background: on ? 'var(--action-primary-bg)' : 'transparent', color: on ? 'var(--action-primary-fg)' : 'var(--text-primary)', transition: 'var(--transition-all)' } }, it); })),
    React.createElement('div', { style: { display: 'flex', alignItems: 'center', gap: 8 } }, right, React.createElement('a', { onClick: onBook, style: { background: 'var(--action-primary-bg)', color: 'var(--action-primary-fg)', padding: '12px 22px', borderRadius: 999, fontSize: 14, fontWeight: 600, cursor: 'pointer' } }, bookLabel)));
}
